import { Component, OnInit, Input, Output, EventEmitter, AfterViewInit, HostListener, HostBinding } from 'angular2/core';

@Component({
    selector: 'IOComponent',
    templateUrl: 'IOComponent/IOComponent.html'
})

export class IOComponent  {
    // constructor() { }

    // ngOnInit() { }
    
    //  @Input() buttonText:string;
    // isDisabled: boolean = true;
    // @Output() logHi: EventEmitter<string>  = new EventEmitter ;
    
    // @HostBinding() propPassedByParent;
    
    // @HostListener("passToChild", ['$event.target.value'])
    // onReceiveFromParent(newValue) {
        
    // }
}
    